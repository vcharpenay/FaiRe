# Relational Reasoning Benchmark

To run experiments, install [PyKEEN](https://pykeen.readthedocs.io/en/stable/) and execute:

```sh
python eval.py
```

Once evaluation is finished, to generate result plots, execute:

```sh
python plot.py
```